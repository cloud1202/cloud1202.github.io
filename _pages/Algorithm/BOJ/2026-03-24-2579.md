---
title: "[BOJ] 2579번 계단 오르기 / C++"
tags:
- C++
- 백준
- BOJ
- 알고리즘
- Algorithm
date: 2026-03-24 09:00 +0900
thumbnail: "/assets/img/thumbnail/book.jpg"
problemUrl: https://www.acmicpc.net/problem/2579
is_post: true
---
## 🔗 [계단 오르기]({{page.problemUrl}})
[{{page.problemUrl}}]({{page.problemUrl}})

---

## 📌 문제 요약

> 계단 오르는 데는 다음과 같은 규칙이 있다.
>
> 계단은 한 번에 한 계단씩 또는 두 계단씩 오를 수 있다. 즉, 한 계단을 밟으면서 이어서 다음 계단이나, 다음 다음 계단으로 오를 수 있다.
> 연속된 세 개의 계단을 모두 밟아서는 안 된다. 단, 시작점은 계단에 포함되지 않는다.
> 마지막 도착 계단은 반드시 밟아야 한다.
> 따라서 첫 번째 계단을 밟고 이어 두 번째 계단이나, 세 번째 계단으로 오를 수 있다. 하지만, 첫 번째 계단을 밟고 이어 네 번째 계단으로 올라가거나, 첫 번째, 두 번째, 세 번째 계단을 연속해서 모두 밟을 수는 없다.
> 각 계단에 쓰여 있는 점수가 주어질 때 이 게임에서 얻을 수 있는 총 점수의 최댓값을 구하는 프로그램을 작성하시오.
>
> **입력: 입력의 첫째 줄에 계단의 개수가 주어진다.**
> **둘째 줄부터 한 줄에 하나씩 제일 아래에 놓인 계단부터 순서대로 각 계단에 쓰여 있는 점수가 주어진다. 계단의 개수는 300이하의 자연수이고, 계단에 쓰여 있는 점수는 10,000이하의 자연수이다.**
> **출력: 첫째 줄에 계단 오르기 게임에서 얻을 수 있는 총 점수의 최댓값을 출력한다.**

---

## 💡 접근 방법

N개의 계단이 있을 때, 각 계단의 점수는 S(N)고, 최댓값은 dp(N) 가정한다.
N번째 계단의 점수 최댓값은 S(N) + S(N-1) + dp(N-3) 이거나, S(N) + dp(N-2) 이다.

dp에 N-3번째 까지는 값이 들어가 있어야 하므로 3개의 값을 미리 넣어주고 그뒤부터 위 계산식으로 더해주면 된다.

---

## ⚠️ 처음에 했던 실수



---

## 💻 코드

```cpp
#include <iostream>
#include <vector>
#include <cmath>
using namespace std;

int N;
vector<int> stair;
vector<int> dp;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  cin >> N;

  for(int i = 0; i < N; ++i)
  {
      int score;
      cin >> score;
      stair.push_back(score);
  }

  dp.push_back(stair[0]);
  dp.push_back(stair[1] + stair[0]);
  dp.push_back(max(stair[2] + stair[1], stair[2] + dp[0]));

  for(int i = 3; i < N; ++i)
  {
      dp.push_back(max(stair[i] + stair[i - 1] + dp[i - 3], stair[i] + dp[i - 2]));
  }
  cout << dp[N - 1];
  return 0;
}
```